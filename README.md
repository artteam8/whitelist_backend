Бэк на fastapi, два эндпоинта -- /map и /ping, на / лежит index.html для дебага (та же карта что в приложении). /map возвращает набор полигонов, в которых нет связи, основываясь на данных из БД. /ping уведомляет сервер, что на данных координатах связь есть.
